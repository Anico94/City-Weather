export function mapIconCodeToImageURL(iconCode: string): string {
  //link to open weather docs https://openweathermap.org/weather-conditions
  return `https://openweathermap.org/img/wn/${iconCode}@2x.png`;
}
